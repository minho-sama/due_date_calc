// eslint-disable-next-line no-unused-vars
function calculateDueDate (submitDate: any, turnaround: number): string {
    
    console.log("yo what is the input submitDate");

    return "string";
}

calculateDueDate("", 5)